pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        flutterPlugins() 
    }
    includeBuild("../packages/flutter_tools/") 
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = 'zxc_GPS3'
include ':app'
